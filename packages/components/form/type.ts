export interface FormProps {
  size?: '' | 'lg' | 'base' | 'sm';
  showColon?: boolean;
  showLabel?: boolean;
}

export interface FormItemProps {
  size?: '' | 'lg' | 'base' | 'sm';
  label?: string;
  showLabel?: boolean;
}
